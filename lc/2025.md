# 1. 经典数据结构算法

## 1. 1手把手刷链表算法

### 双指针技巧秒杀七道链表题目

- 第 23 题 **合并K个有序链表**🟠

```java
Queue<ListNode> queue = new PriorityQueue<>((a,b) -> (a.val - b.val));
queue.add;
queue.poll;
```

- 第19题 **删除链表的倒数第 N 个结点**🟠

无他 背住这个reverse找链表的function提高速度

同理快慢指针判断环是否存在的原理为

 $(x_{\text{fast}} - x_{\text{slow}}) \mod C = 0$  

  $x_{\text{slow}} \mod C = 0$​

```java
    public ListNode findReverseN(ListNode head, int n){
        ListNode front = head;
        for(int i = 0; i < n; i++){
            front = front.next;
        }
        ListNode rear = head;
        while(front != null){
            front = front.next;
            rear = rear.next;
        }
        return rear;
    }
```

### 【强化练习】链表双指针经典习题

- 第82题**删除排序链表中的重复元素 II** 🟠

```java
class Solution {
  	//双指针解法 
    public ListNode deleteDuplicates_1(ListNode head) {
        ListNode dummy = new ListNode(-101);
        dummy.next = head;
        ListNode prev = dummy;
        while(head != null && head.next != null){
            if(head.next.val == head.val){
                while(head.next != null && head.next.val == head.val){
                    head = head.next;
                }
                prev.next = head.next;
            }else{
                prev = prev.next;
            }
            head = head.next;
        }
        return dummy.next;
    }
  	//递归解法
    public ListNode deleteDuplicates(ListNode head) {
        // base case
        if (head == null || head.next == null) {
            return head;
        }
        if (head.val != head.next.val) {
            // 如果头结点和身后节点的值不同，则对之后的链表去重即可
            head.next = deleteDuplicates(head.next);
            return head;
        }
        // 如果如果头结点和身后节点的值相同，则说明从 head 开始存在若干重复节点
        // 越过重复节点，找到 head 之后那个不重复的节点
        while (head.next != null && head.val == head.next.val) {
            head = head.next;
        }
        // 直接返回那个不重复节点开头的链表的去重结果，就把重复节点删掉了
        return deleteDuplicates(head.next);
    }
}
```

- 第1836题**从未排序的链表中移除重复元素**🟠

 	自己做的Beat 100% version

```java
class Solution {
    public ListNode deleteDuplicatesUnsorted(ListNode head) {
        int[] count = new int[100001];
        ListNode p = head;
        while (p != null) {
            count[p.val]++;
            p = p.next;
        }

        ListNode dummy = new ListNode(0, head);
        p = dummy;

        while(p != null && p.next != null){

            ListNode temp = p.next;
            while(temp != null && count[temp.val] != 1){
                temp = temp.next;
            }
            p.next = temp;
            p = p.next;

        }

        return dummy.next;

    }
}
```



- [第264题 丑数 II🟠](https://leetcode.com/problems/ugly-number-ii/submissions/1507861716/)

​	丑数是2，3，5的倍数，且在原丑数的倍数

​	合并2，3， 5三条数组 去重	

```java
class Solution {
    public int nthUglyNumber(int n) {
        // 可以理解为三个指向有序链表头结点的指针
        int p2 = 1, p3 = 1, p5 = 1;
        // 可以理解为三个有序链表的头节点的值
        int product2 = 1, product3 = 1, product5 = 1;
        // 可以理解为最终合并的有序链表（结果链表）
        int[] ugly = new int[n + 1];
        // 可以理解为结果链表上的指针
        int p = 1;

        // 开始合并三个有序链表
        while (p <= n) {
            // 取三个链表的最小结点
            int min = Math.min(Math.min(product2, product3), product5);
            // 接到结果链表上
            ugly[p] = min;
            p++;
            // 前进对应有序链表上的指针
            if (min == product2) {
                product2 = 2 * ugly[p2];
                p2++;
            }
            if (min == product3) {
                product3 = 3 * ugly[p3];
                p3++;
            }
            if (min == product5) {
                product5 = 5 * ugly[p5];
                p5++;
            }
        }
        // 返回第 n 个丑数
        return ugly[n];
    }
}
```

练习

[1201. 丑数 III 🟠](https://leetcode.com/problems/ugly-number-iii)

[263. 丑数 🟢](https://leetcode.com/problems/ugly-number)

[313. 超级丑数 🟠](https://leetcode.com/problems/super-ugly-number)



- [378. 有序矩阵中第 K 小的元素🟠](https://leetcode.com/problems/kth-smallest-element-in-a-sorted-matrix/description/)

  ```java
  class Solution {
      public int kthSmallest(int[][] matrix, int k) {
          // 存储二元组 (matrix[i][j], i, j)
          // i, j 记录当前元素的索引位置，用于生成下一个节点
          PriorityQueue<int[]> pq = new PriorityQueue<>((a, b) -> {
              // 按照元素大小升序排序
              return a[0] - b[0];
          });
  
  
          // 初始化优先级队列，把每一行的第一个元素装进去
          for (int i = 0; i < matrix.length; i++) {
              pq.offer(new int[]{matrix[i][0], i, 0});
          }
  
          int res = -1;
          // 执行合并多个有序链表的逻辑，找到第 k 小的元素
          while (!pq.isEmpty() && k > 0) {
              int[] cur = pq.poll();
              res = cur[0];
              k--;
              // 链表中的下一个节点加入优先级队列
              int i = cur[1], j = cur[2];
              if (j + 1 < matrix[i].length) {
                  pq.add(new int[]{matrix[i][j + 1], i, j + 1});
              }
          }
          return res;
      }
  }
  ```

- [373. 查找和最小的 K 对数字](https://leetcode.com/problems/find-k-pairs-with-smallest-sums/description/)与378同类型 作为练习即可

### 单链表的花式反转方法汇总

- [206. 反转链表](https://leetcode.com/problems/reverse-linked-list/description/) 🟠

  ```java
  class Solution {
      // 双指针翻转链表背住！
      public ListNode reverseList(ListNode head) {
          if(head == null || head.next == null){
              return head;
          }
          ListNode pre = null, cur = head, nxt = head.next;;
          while(cur != null){
              
              cur.next = pre;
              pre = cur;
              cur = nxt;
              if(nxt != null){
                  nxt = nxt.next;
              }
          }
  
          return pre;
      }
      // 递归方法
      public ListNode reverseList(ListNode head) {
          if(head == null || head.next == null){
              return head;
          }
          ListNode rest = reverseList(head.next);
          head.next.next = head;
          head.next =  null;
          return rest;
      }
  }
  ```

- [92. 反转链表 II](https://leetcode.com/problems/reverse-linked-list-ii/)🟠

  ```java
  class Solution {
      // 两种不同的解法需要时常复习 下面是递归
      ListNode successor;
      public ListNode reverseBetween(ListNode head, int left, int right) {
          if(left == 1){
              return reverse(head, right);
          }
          head.next = reverseBetween(head.next, left - 1, right - 1);
          return head;
      }
  
      public ListNode reverse(ListNode head, int n){
          if(n == 1){
              successor = head.next;
              return head;
          }
  
          ListNode last = reverse(head.next, n - 1);
          head.next.next = head;
          head.next = successor;
          return last;
      }
  
      //下面是迭代
      public ListNode reverseBetween(ListNode head, int left, int right) {
          if(left == 1){
              return reverseN(head, right); // 从1开始直接reverseN
          }
          ListNode pre = head;
  
          for(int i = 0; i < left - 2; i++){
              pre = pre.next;
          }
  
          pre.next = reverseN(pre.next, right - left + 1);
          return head;
      }
  
      public ListNode reverseN(ListNode head, int n){
          if(head == null || head.next == null){
              return head;
          }
  
          ListNode pre = null, cur = head, nxt = head.next;
  
          while(cur != null && n > 0){
              cur.next = pre;
              pre = cur;
              cur = nxt;
              if(nxt != null){
                  nxt = nxt.next;
              }
              n--;
          }
          head.next = cur; //最后讲头结点（最后的末尾节点）接在剩余的list上
          return pre;
      }
  }
  ```

- 练习：[25. K 个一组翻转链表](https://leetcode.com/problems/reverse-nodes-in-k-group/description/)



### 如何判断回文链表

- 练习： [234. 回文链表](https://leetcode.com/problems/palindrome-linked-list/description/)



## 1. 2手把手刷数组算法

### 双指针技巧秒杀七道数组题目

[ 167, 26, 27, 283, 344, <span style="color: red;">5</span>, 83]

### 二维数组的花式遍历技巧

[ 151,  48, 54, 59, 61]

### 一个方法团灭 nSum 问题

[ 1, 15, 167, <span style="color: red;">18</span>]

```java
class Solution {
    public List<List<Integer>> fourSum(int[] nums, int target) {
        Arrays.sort(nums);
        return nSumTarget(nums, 4, 0, target);
    }
    public List<List<Integer>> nSumTarget(int[] nums, int n, int start, long target){
        int sz = nums.length;
        List<List<Integer>> res = new LinkedList<>();
        if(n < 2 || sz < n) return res;

        if(n == 2){
            int lo = start, hi = nums.length - 1;
            while(lo < hi){
                int left = nums[lo], right = nums[hi];
                int sum = left + right;
                if(sum == target){
                    res.add(new LinkedList<>(Arrays.asList(left, right)));
                    while(lo < hi && nums[lo] == left) lo++;
                    while(lo < hi && nums[hi] == right) hi--;
                }

                if(sum < target) lo++;
                if(sum > target) hi--;
            }
        }else{
            for(int i = start; i < sz - n + 1; i++){
                int cur = nums[i];
                List<List<Integer>> temp = nSumTarget(nums, n - 1, i + 1, target - cur);
                for(List<Integer> solution : temp){
                    solution.add(cur);
                    res.add(solution);
                }
                while(i < sz - n + 1 && nums[i] == nums[i + 1]) i++;
            }
        }
        return res;
    }
}
```



### 小而美的算法技巧：前缀和数组

[ 303,  304]



### 小而美的算法技巧：差分数组

[ 1094, 1109, <span style='color: yellow;'>370</span>]



### 滑动窗口算法核心代码模板

[ 3, 438, 567, 76]



### Rabin Karp 字符匹配算法， 不是很频繁感觉但是要掌握

[ <span style='color: yellow;'>187</span>, <span style='color: red;'>28</span>]

本质就是字符匹配哈希算法。

数学中-10 % 7 为4, Java 里 -10 % 7 为 -3. 这两个是完全不同的东西，所以代码要保证被模数不能为负

$$(a - b) \mod Q = (a \mod Q - b \mod Q + Q) \mod Q$$​



### 二分搜索算法核心代码模板

[ <span style='color: white;'>34</span>, <span style='color: white;'>704</span>]



### 带权重的随机选择算法

[ <span style='color: white;'>528</span>]



### 田忌赛马背后的算法决策

[ <span style='color: white;'>870</span>] //最大的都比不过直接塞个fw上来能比得过的之后不用再比



### 实际运用二分搜索时的思维框架【练习】

[ 1011, 410, 875]



### 【强化练习】数组双指针经典习题

[ 80, 125, 75, 88, 977, 1329, 1260, 867, 14]



### 【强化练习】前缀和技巧经典习题

[ 1314, 724, 238, 1352, 525, 523, 560, 974, 1124]



### 【强化练习】滑动窗口算法经典习题

[ 1658, 713, 1004, 424, 219, 220, 209, 395]



### 【强化练习】二分搜索算法经典习题

[ 566, 74, 240, 392, 792, 658, 35, 162, 852, 33, 81]



## 1.3 经典列，栈算法



### 队列实现栈以及栈实现队列

[ <span style='color: white;'>225</span>， <span style='color: white;'>232</span>]

队列实现栈就是循环添加，栈实现队列就是左手倒右手



### 单调栈算法模板解决三道例题

[ <span style='color: white;'>496</span>， <span style='color: red;'>503</span>,  <span style='color: white;'>739</span>]

```
503 单调栈处理环形数组直接取模模拟double之后的数组
class Solution {
    public int[] nextGreaterElements(int[] nums) {
        int n = nums.length;
        int[] res = new int[n];
        Stack<Integer> s = new Stack<>();
        // 数组长度加倍模拟环形数组
        for (int i = 2 * n - 1; i >= 0; i--) {
            // 索引 i 要求模，其他的和模板一样
            while (!s.isEmpty() && s.peek() <= nums[i % n]) {
                s.pop();
            }
            res[i % n] = s.isEmpty() ? -1 : s.peek();
            s.push(nums[i % n]);
        }
        return res;
    }
}
```



# 5. 其他常见算法技巧

## 1. 数学运算技巧

### 1.1 如何高效寻找素数

- [204. 计数质数🔴](https://leetcode.com/problems/count-primes/description/)

```java
class Solution {
    public int countPrimes(int n) {
        Boolean[] isPrime = new Boolean[n];
        Arrays.fill(isPrime, true);
        //到 sqrt n 即可
        for(int i = 2; i * i < n; i++ ){
            if(isPrime[i]){
                // 之前的肯定被遍历过，忽略
                for(int j = i * i; j < n; j += i){
                    isPrime[j] = false;
                }
            }
        }

        int count = 0;
        for(int i = 2; i < n; i++){
            if(isPrime[i]){
                count++;
            }
        }
        return count;
    }
}

```

$T(n) = n \cdot \sum_{p \text{ is prime}} \frac{1}{p} \approx n \cdot \log\log n$

​	•	质数分布的稀疏性决定了倒数和是 \log\log n 。

​	•	每个质数标记 n / p 次，总体乘以 n 。

